import React from 'react';

// Header is now integrated into HeroSection and is kept as a minimal export
// to avoid breaking imports, but is no longer rendered on the home page.
export const Header: React.FC = () => {
  return null;
};
